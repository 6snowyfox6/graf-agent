# Skill: llm_server_setup_windows_linux

## Known setup
- server may run on Windows
- client may run on Ubuntu
- OpenAI-compatible API
- model IDs must match `/v1/models`

## Checklist
- verify `BASE_URL`
- avoid proxy interference using `no_proxy`
- verify `/v1/models`
- use exact returned model IDs