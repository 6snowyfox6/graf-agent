---
trigger: always_on
---

# Python rules

- Use pathlib instead of fragile string paths
- Prefer small, local changes
- Preserve type hints
- Handle `None` safely in label-processing code
- Never introduce OS-specific code unless configurable
- Keep functions readable and testable